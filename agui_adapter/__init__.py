"""Hermes AG-UI adapter.

Exposes the Hermes ``AIAgent`` over the AG-UI protocol (HTTP/SSE): it streams a
Hermes turn as AG-UI events and supports the client-side ("frontend") tool
execution pattern.
"""
